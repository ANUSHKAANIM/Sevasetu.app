// Stub for the `server-only` package used only under the Vitest test runner
// (which doesn't use the `react-server` resolve condition Next.js uses to
// swap this module out for a no-op automatically). See vitest.config.ts.
export {};
